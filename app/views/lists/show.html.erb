<h1><%= @list.name %></h1>
<ul>
  <% @list.movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>
<p><%= link_to "Add a movie to this list",  new_list_bookmark_path(@list), class: "btn btn-primary" %></p>
<p><%= link_to "All lists", lists_path, class: "btn btn-primary" %></p>
